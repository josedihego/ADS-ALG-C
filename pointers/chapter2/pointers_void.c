#include<stdio.h>
#include<stdlib.h>

int main(){
    //  mesma representação e espaço que um ponteiro para char
    // um ponteiro para void nunca será igual a nenhum outro
    // A MENOS que aos dois seja atribuído NULL
    void * pv;
}