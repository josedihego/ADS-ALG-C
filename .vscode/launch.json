{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug Active File (Cross-Platform)",
            "type": "lldb",
            "request": "launch",
            "program": "${fileDirname}/${fileBasenameNoExtension}",
            "args": [],
            "cwd": "${fileDirname}",
            "preLaunchTask": "C/C++: Build Active File"
        }
    ]
}